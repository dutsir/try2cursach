__pycache__/
*.py[cod]
*.pyo
*.egg-info/
*.egg
dist/
build/
*.whl
docs/
# Frontend (Node / Vite / Tailwind)
node_modules/
frontend/dist/
frontend/.vite/
# Vite временные предсборки зависимостей
**/.vite/deps/
**/.vite/deps_temp_*/
# Логи npm / yarn / pnpm
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.npm/
.pnpm-store/
# Локальные env-файлы фронта
*.local
frontend/.env
frontend/.env.local

venv/
.venv/
env/

.env
*.sqlite3
db.sqlite3

*.log
celerybeat-schedule
celerybeat.pid

staticfiles/
media/

.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

/backups/
*.dump
*.sql
!docs/**/*.sql

# Локальные профили Chrome (Qrator/Ozon cookies) — не коммитить
var/

# Кэш тестов и IDE-артефакты
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
coverage.xml

# Claude / Cursor worktrees (локальные копии, не часть проекта)
.claude/
*.env.bak.*
*.txt
*.md
# Модели sentence-transformers (скачиваются при первом rebuild_embeddings)
.cache/
models/
*.sh
# Инфра-скрипты Docker нужны в образе/на VPS (deploy.sh делает git pull → build).
!docker/*.sh
*.ps1
design_handoff_scout/