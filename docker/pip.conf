[global]
# Используем только бинарные wheels (без компиляции из исходников)
only-binary = torch
# Для torch используем PyTorch индекс
find-links = https://download.pytorch.org/whl/torch_stable.html

[install]
# Не пересобирать зависимости если они уже есть
no-build-isolation = false
